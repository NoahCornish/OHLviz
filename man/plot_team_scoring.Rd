% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_team_scoring.R
\name{plot_team_scoring}
\alias{plot_team_scoring}
\title{Plot Team Scoring}
\usage{
plot_team_scoring(
  data,
  team = NULL,
  n = 10,
  player_col = "Name",
  team_col = "Team",
  goals_col = "G",
  assists_col = "A",
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  goals_colour = "#142D4E",
  assists_colour = "#2474B5"
)
}
\arguments{
\item{data}{A data frame with one row per player for one season.
Goals and assists must cover the same reporting scope.}

\item{team}{Team name to select. If \code{NULL}, the data must contain
exactly one team.}

\item{n}{Maximum number of players to display. Defaults to 10.}

\item{player_col}{Name of the player-name column.}

\item{team_col}{Name of the team column.}

\item{goals_col}{Name of the numeric goals column.}

\item{assists_col}{Name of the numeric assists column.}

\item{title}{Chart title. If \code{NULL}, uses the selected team name.}

\item{subtitle}{Optional chart subtitle.}

\item{caption}{Optional chart caption.}

\item{goals_colour}{Fill colour for goals.}

\item{assists_colour}{Fill colour for assists.}
}
\value{
A ggplot object.
}
\description{
Compare players on one team using stacked goals and assists.
Defaults match the column names returned by OHLpkg.
}
\details{
Players are ranked by goals plus assists, from highest to lowest.
Ties preserve input row order. Rows with missing or non-finite goals
or assists are omitted with a warning.

Goals and assists must be non-negative whole numbers.
Duplicate player names within the selected team are rejected:
resolve duplicate records or supply unique player labels first.

Selecting a team filters rows; it does not convert season totals
into statistics earned only with that team. Supply team-specific
statistics when that distinction matters.
}
\examples{
players <- data.frame(
  Name = c("Player A", "Player B", "Player C"),
  Team = rep("Example Team", 3),
  G = c(25, 32, 18),
  A = c(40, 25, 30)
)

plot_team_scoring(players)
plot_team_scoring(players, team = "Example Team", n = 2)
}
